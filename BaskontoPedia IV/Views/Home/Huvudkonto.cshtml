@model BaskontoPedia_IV.Controllers.AccountViewModel

@{
    ViewBag.Title = "Huvudkonto";
}
<div class="container">
    <div class="row">
        @if (Model.MainAccount != null)
        {
        <h2>
            @(Model.MainAccount.AccountId)x @Model.MainAccount.Name
            @if (Model.MainAccount.LastYear != "2017")
            {
            <strong>(Kontot saknas i BAS 2017) @Model.MainAccount.LastYear </strong>
            }
        </h2>
        }
        else
        {
            <h2>Kontot saknas i BAS 2017</h2>
        }
    </div>
</div>

<div class="e-line">

</div>

<div class="container">
    <div class="row">


        <div class="row">
            <div class="col-md-6">
                <h4>Kontots innehåll</h4>
                @if (!string.IsNullOrEmpty(Model.MainAccount.SidaIBokforingsboken))
    {
            <p>
                Se Svensk Redovisning Bokföringssboken 2018, s. @Model.MainAccount.SidaIBokforingsboken.
            </p>
}

                @if (!string.IsNullOrEmpty(Model.MainAccount.SidaIBokslutsboken))
    {
            <h4>Kontots innehåll</h4>
            <p>
                Se Svensk Redovisning Bokslutsboken 2018, s. @Model.MainAccount.SidaIBokslutsboken.
            </p>
}
                @if (Model.History != null)
    {
            <h4>Tidigare benämningar</h4>
            <table class="table">
                <tr>
                    <th>
                        År
                    </th>
                    <th>
                        Namn
                    </th>
                </tr>

                @foreach (var item in Model.History)
    {
            <tr>
                <td>
                    @item.Years
                </td>
                <td>
                    @item.Name
                </td>
            </tr>
}
            </table>
}

                <h4>Underkonton</h4>
                <table class="table">
                    <tr>
                        <th>Kontonr</th>
                        <th>Namn</th>
                        <th>Kommentar</th>
                    </tr>

                    @foreach (var item in Model.SubAccounts)
        {
            <tr>
                <td>
                    @Html.ActionLink(item.AccountID, "Huvudkonto/" + item.AccountID, "Home")
                    @*@Html.DisplayFor(modelItem => item.AccountID)*@
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>Gruppkonto</td>
            </tr>
}

                </table>


                <h4 data-toggle="tooltip" title="Enligt användningsundersökning KA 18">Underkonton i praktisk användning</h4>
                <table class="table">
                    <tr>
                        <th>Kontonr</th>
                        <th>Namn</th>
                        <th>Antal ftg</th>
                    </tr>

                    @foreach (var item in Model.UsedAccounts)
        {
            <tr>
                <td>
                    @item.AccountID
                    @*@Html.DisplayFor(modelItem => item.AccountID)*@
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
            </tr>
}

                </table>
            </div>
            <div class="col-md-6">
                <div class="panel panel-default">
                    <div class="panel-heading">Företagsformer</div>
                    <div class="panel-body">
                        <ul>
                            <li>Aktiebolag</li>
                            <li>Enskild firma</li>
                        </ul>
                    </div>
                </div>

                <div class="panel panel-default">
                    <div class="panel-heading">Placering i kontoplanen</div>
                    <div class="panel-body">
                        <p>@Model.Class.AccountId @Model.Class.Name</p>
                        <p>&nbsp;&nbsp;@Html.ActionLink(Model.Group.AccountId + " " + Model.Group.Name, "Kontogrupp/" + Model.Group.AccountId, "Home")</p>
                        @if (Model.MainAccount != null)
                        {
                            @*<p>&nbsp;&nbsp;&nbsp;&nbsp;@Model.MainAccount.MainAccount @Model.MainAccount.MainAccount</p>*@
                        }
                    </div>
                </div>

                <div class="panel panel-default">
                    <div class="panel-heading" data-toggle="tooltip" title="(INK2 2017 P3)">SRU-kod</div>
                    <div class="panel-body">
                        <p>TBD!</p>
                    </div>
                </div>

                @if (Model.MainAccount.XbrlElements.Count > 0)
                {
                <div class="panel panel-default">
                    <div class="panel-heading" data-toggle="tooltip" title="(Enligt bolagsverkets taxonomi 2017-09-30)">Tillhör följande XBRL-element</div>
                    <div class="panel-body">
                        <div class="container-fluid">
                            @foreach (var e in Model.MainAccount.XbrlElements)
                            {
                            <div class="row">
                                <div class="col-md-6">
                                    <h5><strong>Elementnamn</strong></h5>
                                    <p>@Html.ActionLink(e.ElementName, "Xbrl/" + e.ElementName, "Home")</p>
                                            <h5><strong>Rubriktext</strong></h5>
                                            <p>@(e.Name)</p>
</div>
                                <div class="col-md-6">
                                    <h5><strong>Placering</strong></h5>
                                    <p>@(e.Header1)</p>
                                    <p>@(e.Header2)</p>
                                    <p>@(e.Header3)</p>
                                    <p>@(e.Header4)</p>
                                </div>

                            </div>
                            }

                            </div>
                    </div>
                </div>
                }

                <div class="panel panel-default">
                    <div class="panel-heading">Användning (KA 18) (för relevanta företagsformer)</div>
                    <div class="panel-body">
                        <table class="table">
                            <tr>
                                <th>Förekomst</th>
                                <th>Användn</th>
                                <th>Användn D</th>
                                <th>Användn K</th>
                            </tr>
                        </table>
                        <p>TBD!</p>
                    </div>
                </div>

                <div class="panel panel-default">
                    <div class="panel-heading">Alternativ benämning (KA 18)</div>
                    <div class="panel-body">
                        <p>TBD!</p>
                    </div>
                </div>

            </div>
        </div>
    </div>

</div>